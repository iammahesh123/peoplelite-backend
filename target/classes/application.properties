spring.application.name=hr-lite

spring.jpa.open-in-view=false
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.default-property-inclusion=non_null

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:maheshkadambala7@gmail.com}
spring.mail.password=${MAIL_PASSWORD:tgpootkgxazkpcf}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

server.port=8082

app.jwt.secret=${JWT_SECRET:default-dev-secret-key-must-be-at-least-256-bits-long-for-hmac-sha256}
app.jwt.access-token-expiry-ms=900000
app.jwt.refresh-token-expiry-ms=604800000

app.cors.allowed-origins=${CORS_ORIGINS:http://localhost}

app.storage.bucket-name=${S3_BUCKET:hr-lite-docs}
app.storage.endpoint=${S3_ENDPOINT:http://localhost:9000}
app.storage.access-key=${S3_ACCESS_KEY:minioadmin}
app.storage.secret-key=${S3_SECRET_KEY:minioadmin}
app.storage.region=${S3_REGION:us-east-1}

app.google.client-id=${GOOGLE_CLIENT_ID:384910119451-19niqmgqalj5upk044s5hjms8636n1f2.apps.googleusercontent.com}

spring.main.allow-bean-definition-overriding=true

# Razorpay Payment Gateway
app.razorpay.key-id=${RAZORPAY_KEY_ID:}
app.razorpay.key-secret=${RAZORPAY_KEY_SECRET:}
app.razorpay.webhook-secret=${RAZORPAY_WEBHOOK_SECRET:}

# Subscription
app.subscription.trial-days=30
app.subscription.grace-period-days=7

spring.profiles.active=local


